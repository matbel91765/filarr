# Filarr — local-only build
#
# This file is intentionally minimal. The public source tree does not ship
# with any cloud, billing or telemetry integration; forks that wish to add
# them should document their own variables here.

NODE_ENV=development
