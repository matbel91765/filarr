# Dependencies
node_modules

# Build outputs
dist
build
dist-electron
storybook-static
design-showcase-static

# Cloudflare — état LOCAL de wrangler (D1 en SQLite, R2/KV simulés, caches).
# `wrangler d1 migrations apply --local` le crée dans infra/cloudflare-worker/ :
# ce sont des fichiers de base de données, avec les lignes semées pendant les
# essais. Rien de tout cela n'a sa place dans le dépôt.
.wrangler/
infra/cloudflare-worker/.admin-preview/

# Test & coverage artifacts
coverage
test-results

# Logs
*.log
build-output.txt
fix-typescript-errors.txt
test-results.txt

# OS
.DS_Store

# Environment (secrets)
.env
infra/docker/.env
infra/k8s/secrets.yaml
secret.key

# IDE / tools
.claude/settings.local.json
.claude

# Test fixtures
e2e/fixtures/large-file.zip
packages/marketing-site/.next
